<div id="fadein">
  <div class="container-fluid index-products" style = "background: linear-gradient(-225deg, rgba(201, 48, 44, 0) 0%, rgba(255, 235, 0, 0) 100%), url('http://res.cloudinary.com/cavedesmoines/image/upload/v1473258751/index-products_jkifoo.jpg');">
    <%= render '/shared/navbar' %>
    <div class="row">
      <div class="col-xs-12 col-sm-2">
          <%= render '/shared/news' %>
      </div>
      <div class="col-xs-12 col-sm-8">
        <h2 class="cat-beer">Vous partiriez sur :</h2>
        <div class="container-fluid">
          <div class="row">
            <div>
              <% if @products.count != 0 %>
                <% @products.each do |product| %>
                  <div class="col-xs-12 col-sm-4">
                    <div class="card-show-index">
                      <%= render partial: '/shared/product', locals: { product: product } %>
                    </div>
                  </div>
                <% end %>
              <% else %>
              <div class="no-product text-center">
                Pas de résultat immédiat pour cette recherche, appelez-nous au 09.81.02.42.42 pour qu'on vous commande ce produit chez un fournisseur !
              </div>
              <div class="random-choice text-center">
                En si on essayait... un article au hasard !<br />
                <%= cl_image_tag("icons/random-icon.png", class: 'random-icon') %>
              </div>
              <% end %>
            </div>
          </div>
        </div>
      </div>
      <div class="col-xs-12 col-sm-2 refine-search">
      <%= render 'shared/refine-search' %>
      </div>
    </div>
  </div>
  <%= render '/shared/footer' %>
</div>
