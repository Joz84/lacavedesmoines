<h2 style="margin-bottom:16px">Affiner votre recherche</h2>
<%= simple_form_for :research, url: products_path, html: { class: "refine-word"}, method: :get do |f| %>
  <%= f.error_notification %>
  <%= f.input :color, collection: @colors, label: 'Couleur' %>
  <%= f.input :kind, collection: @kinds, label: 'Type' %>
  <%= f.input :brewery, collection: @breweries, label: 'Brasserie' %>
  <%= f.input :min_price, collection: @prices, label: 'Prix min' %>
  <%= f.input :max_price, collection: @prices, label: 'Prix max' %>
  <%= f.button :submit, class: 'btn-outline-secondary refine-search-button', value: "Rechercher" %>
  <% end %>
